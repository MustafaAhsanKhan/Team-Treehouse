import logging

# critical, error, warning, info, debug, notset

logging.warn("ERROR")

#logging.basicConfig(filename='', level=logging.debug)  # An example how we would log an error to a file

logging.debug("Debug error")